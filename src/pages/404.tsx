import React, { FunctionComponent } from 'react';
import {NotFound} from "@/components/NotFound";

interface OwnProps {}

type Props = OwnProps;

const NotFoundPage: FunctionComponent<Props> = () => <NotFound/>;

export default NotFoundPage;
